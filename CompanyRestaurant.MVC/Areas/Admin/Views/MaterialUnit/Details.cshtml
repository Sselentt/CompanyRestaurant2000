@model CompanyRestaurant.MVC.Models.MaterialUnitVM.MaterialUnitViewModel

@{
    ViewData["Title"] = "Malzeme Birimi Detayları";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>@ViewData["Title"]</h2>

<div>
    <h4>Malzeme Birimi Detayları</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Unit)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Unit)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.MaterialId)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.MaterialId)
        </dd>
    </dl>
    <p>
        <a href="@Url.Action("Edit", new { id = Model.Id })" class="btn btn-primary">Düzenle</a>
        <a href="@Url.Action("Index")" class="btn btn-secondary">Geri</a>
    </p>
</div>
